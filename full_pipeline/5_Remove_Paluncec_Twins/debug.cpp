#include"Check.h"
#include<iostream>
#include<vector>
#include<list>
#include<string>
/*
inline length_t Delete(length_t number, digit dig);

void Mutate(length_t codeword, digit length, digit del, vector<length_t>& mcodewords);

bool Check(length_t first, length_t second, digit length, digit del); //adapted from Tran's algorithm
*/

int heaven()
{
	//for (int i = 0; i < 6; i++)
		//cout << Digit(fromQuad("302101"), i) << endl;
	//cout << Check2(fromQuad("0123012302101"), fromQuad("1230020302101"), 13, 2) << endl;
	system("pause");
	return 0;
}